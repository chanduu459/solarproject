import 'package:flutter/material.dart';

class IssuesTab extends StatelessWidget {
  const IssuesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Issues Management'));
  }
}

